version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.12"

python:
  install:
     - method: pip
       path: .
       extra_requirements:
           - dev

sphinx:
  builder: html
  fail_on_warning: true
