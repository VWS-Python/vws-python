language: "python"

python:
  - "3.5"

install:
  - "pip install --upgrade .[dev]"

cache: "pip"

before_script:
    - "flake8 ."
    - "prospector --strictness veryhigh"
    - "mypy src"

script:
    - "pytest --cov=src"

after_success:
    - "coveralls"
